---
name: sdd-workflow-init
description: Initialize SDD workflow for a project by scanning source code, README, CLAUDE.md, and configuration files to create a project-specific spec planning template. Run once per project to establish the planning foundation. The generated template is used automatically by /plan-spec.
user-invocable: true
---

# SDD Workflow Init

Analyze a project and create a project-specific spec planning template pre-populated with detected domain context, toolchain, testing patterns, and coding conventions.

**Input**: `/sdd-workflow-init`

No arguments. Runs in the current project directory.

**Output**: `.claude/sdd-workflow/spec-planning-template.md`

---

## Step 1: Scan project sources

Read these in parallel using an Explore agent:

**Identity files:**
- `README.md` (or `readme.md`, `Readme.md`)
- `CLAUDE.md`, `.claude/CLAUDE.md`, `.claude/settings.json`
- `.copilot-instructions.md`
- `CONTRIBUTING.md`, `DEVELOPMENT.md`
- `docs/` directory listing (one level)

**Build and dependency files:**
- `package.json`, `tsconfig.json` (Node/TypeScript)
- `pyproject.toml`, `setup.py`, `setup.cfg`, `requirements.txt` (Python)
- `Cargo.toml` (Rust)
- `go.mod` (Go)
- `Makefile`, `justfile`, `Taskfile.yml`
- Monorepo: `pnpm-workspace.yaml`, `lerna.json`, `nx.json`, `turbo.json`

**Configuration files:**
- `.env.example`, `.env.template` (environment patterns)
- `docker-compose.yml`, `Dockerfile`
- CI config: `.github/workflows/*.yml`, `.gitlab-ci.yml`, `Jenkinsfile`
- Linter config: `.eslintrc*`, `ruff.toml`, `pyproject.toml [tool.ruff]`, `.clippy.toml`
- Type-check config: `tsconfig.json`, `mypy.ini`, `pyrightconfig.json`

**Source structure:**
- Top-level directory listing
- `src/` or `lib/` listing (one level deep)
- Test directory: `tests/`, `test/`, `__tests__/`, `spec/` (structure only)

**Do NOT read:**
- Every source file (scan structure, not content)
- `.env` files (may contain secrets)
- `node_modules/`, `venv/`, `.git/`, or other generated directories

---

## Step 2: Extract project profile

From the scanned sources, identify:

### Language and framework
- Primary language(s) and version constraints
- Frameworks (web, CLI, library, data pipeline, etc.)
- Key dependencies and their purpose (from dependency files)

### Toolchain
| Tool | How detected | Command |
|------|-------------|---------|
| Package manager | dependency file type | `npm`, `pip`, `cargo`, etc. |
| Build | scripts in package.json, Makefile targets | `npm run build`, `make`, etc. |
| Test | test framework in dependencies + config | `pytest`, `jest`, `go test`, etc. |
| Lint | linter config files or scripts | `eslint`, `ruff`, `clippy`, etc. |
| Type-check | type-checker config or scripts | `tsc`, `mypy`, `pyright`, etc. |
| Entry point | main field, scripts, or conventional files | `src/index.ts`, `main.py`, etc. |

### Architecture patterns
- Module organization (monorepo, flat, layered, hexagonal, etc.)
- Key patterns visible in structure (provider pattern, middleware, plugins, MVC, etc.)
- API boundaries (REST, GraphQL, CLI, library, gRPC, etc.)

### Domain context
- What the project does (from README, first paragraph)
- Key domain terminology (from README, docs, module names)
- Domain-specific constraints mentioned in docs or CLAUDE.md

### Conventions
- Naming conventions (from file names, module structure)
- Error handling patterns (from CLAUDE.md, CONTRIBUTING.md)
- Configuration patterns (env vars, config files, feature flags)
- Coding standards documented in CLAUDE.md or CONTRIBUTING.md

---

## Step 3: Generate project-specific template

```bash
mkdir -p .claude/sdd-workflow
```

**If `.claude/sdd-workflow/spec-planning-template.md` already exists:** Ask the user before overwriting. Show a summary of what changed since the last generation.

Start from the base template (`templates/spec-planning-template.md` in this plugin) and extend it with detected context. The project-specific template keeps all base template sections intact and adds project-specific content.

Write `.claude/sdd-workflow/spec-planning-template.md`:

```markdown
# Spec Planning Template — <project-name>

> Generated by `/sdd-workflow-init` on <date>. Edit freely — this is your project's planning foundation.
> Re-run `/sdd-workflow-init` to regenerate (will ask before overwriting).

## What to build
<1-3 sentences describing the feature or change. Be specific about the outcome.>

### Source references
- **JIRA tickets:** <PROJ-123 — acceptance criteria are authoritative when active>
- **Roadmap epic:** <Epic N from docs/Roadmap-PhaseN.md>
- **Existing spec:** <openspec/changes/<name>/>

## Why now
<What is this blocking? Why is this the next priority?>

## Research needed
<What to investigate before designing? Be specific about unknowns.>

## Known constraints
- Primary language: <detected>
- Framework: <detected>
- Tests must pass: `<detected test command>`
- Lint must pass: `<detected lint command>`
<- Type-check must pass: `<detected type-check command>`>
<Additional constraints from CLAUDE.md or project docs>
<Edit and extend as needed for each change.>

## Design preferences
<Architectural opinions. Reference existing patterns in this project.>
- This project uses: <detected patterns, e.g., "provider pattern", "middleware chain", "plugin architecture">
- Module organization: <detected, e.g., "layered: src/models/, src/services/, src/api/">
<Edit and extend as needed for each change.>

## Quality requirements
- [ ] All tasks must have concrete acceptance criteria (verifiable pass/fail)
- [ ] Include test-creation tasks for every module (unit + integration)
- [ ] Tests must reference specific spec scenarios they validate
- [ ] Include manual smoke test tasks for end-to-end validation
<- [ ] Must pass lint: `<detected lint command>`>
<- [ ] Must pass type-check: `<detected type-check command>`>
<- [ ] CI pipeline must pass (detected: <CI system>)>
<Add domain-specific quality gates as needed.>

## Domain context
<Detected from project sources — edit to add nuance.>
- **What this project does:** <from README>
- **Key domain terms:** <extracted from README, docs, module names>
- **Architecture:** <detected patterns and boundaries>
- **Conventions:** <from CLAUDE.md, CONTRIBUTING.md>

## Project toolchain
| Tool | Command |
|------|---------|
| Build | `<detected or "N/A">` |
| Test | `<detected or "N/A">` |
| Lint | `<detected or "N/A">` |
| Type-check | `<detected or "N/A">` |
| Entry point | `<detected or "N/A">` |

## Process instructions
This should be a back-and-forth conversation:
1. **Research first** — investigate unknowns before proposing anything. Read the actual codebase.
2. **Come back with questions** — present options, trade-offs, and ask for input on design decisions.
3. **Challenge your own defaults** — dig deeper if the textbook answer seems too simple.
4. **Review your own work** — audit for completeness, coherence, and gaps.
5. **Generate all artifacts** — proposal, design, specs, tasks, and test tasks.
```

---

## Step 4: Update CLAUDE.md with SDD workflow context

The `/implement-beads` orchestrator reads `CLAUDE.md` in Step 0a to discover toolchain commands. Persisting detected commands there means they survive across sessions without re-detection — and avoids an orchestrator skipping plugin scripts it mistakenly believes are stubs based on stale project notes.

Check whether `CLAUDE.md` exists at the project root:
- If it does **not** exist: create it with only the section below.
- If it exists: check for an existing `## SDD Workflow` section. Update it if present; append it if not.

**Never overwrite or modify any existing CLAUDE.md content** outside the `## SDD Workflow` section.

```markdown
## SDD Workflow

Managed by the `sdd-workflow` Claude Code plugin. Update by re-running `/sdd-workflow-init`.

### Toolchain commands
| Tool | Command |
|------|---------|
| Install deps | `<detected>` |
| Run tests | `<detected>` |
| Lint | `<detected or "not configured">` |
| Type-check | `<detected or "not configured">` |
| Code generation | `<detected — e.g., ./generate-grpc-code.sh — or "not configured">` |
| Entry point | `<detected or "not configured">` |

### Plugin notes
- `sync-openspec-tasks.py` — syncs closed Beads to tasks.md after each wave and at pre-commit. **This script is functional.** Do not skip it based on any stale notes elsewhere in this file.
- `/spec-completion-auditor` — mandatory after all beads close; catches ref mismatches the sync script cannot resolve.
- Generated files (gRPC stubs, protobuf, codegen output) are typically gitignored. After a worktree merge, regenerate using the code generation command above before running tests.
```

---

## Step 5: Report results

Present a summary:

```
## SDD Workflow Initialized

### Project profile
- **Language:** <detected>
- **Framework:** <detected>
- **Test command:** `<detected>`
- **Lint command:** `<detected>`
- **Architecture:** <detected patterns>
- **Domain:** <one-line summary>

### Created
- `.claude/sdd-workflow/spec-planning-template.md`
- `CLAUDE.md` — `## SDD Workflow` section added or updated

### What to do next
1. **Review the template** — edit domain context and constraints to add nuance
2. **Review CLAUDE.md** — verify detected toolchain commands are correct; edit if needed
3. **Start planning** —`/plan-spec <change-name>` uses this template automatically
4. **Quick start from JIRA** —`/spec-from-tickets PROJ-123` for well-defined tickets
5. **Manual planning** — copy the template for a specific change with the `new-plan.sh` script from this plugin's `scripts/` directory
```

---

## Guardrails

- **Don't over-detect.** If something isn't clear from project sources, leave it as a placeholder. Wrong context is worse than no context.
- **Don't read every source file.** Scan structure and representative files, not the entire codebase. This is a quick setup, not an audit.
- **Respect .gitignore.** Don't read or reference content from gitignored files (they may contain secrets).
- **Ask before overwriting.** If the project template already exists, show what's different and let the user decide.
- **The template is a starting point.** Tell the user to edit it — the detected values are best guesses, not gospel.

ARGUMENTS: $ARGUMENTS
